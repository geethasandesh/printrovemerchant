# Backend API Configuration
# IMPORTANT: Set this to your backend URL
VITE_APP_API_URL=http://localhost:5001/api

# INSTRUCTIONS:
# 1. Create new file: pt-merchant/.env (or .env.local)
# 2. Copy the environment variable above into it
# 3. Update VITE_APP_API_URL if your backend runs on a different port
# 4. Save the file
# 5. Restart the development server: npm run dev

